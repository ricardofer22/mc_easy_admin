services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    ports:
      - "3000:3000"
      - "25565:25565"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
